import React from 'react';
import useTitle from '../../hooks/useTitle';

const Inventory = () => {
    useTitle('Inventory')
    return (
        <div>
            Inventory Page
        </div>
    );
};

export default Inventory;